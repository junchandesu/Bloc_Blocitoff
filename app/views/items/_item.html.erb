<%= content_tag :div, class: 'media', id: "item-#{item.id}" do %>
	<p><%= item.name %>
	<%= link_to "" , [current_user, item] , method: :delete, class: 'glyphicon glyphicon-ok', remote: true %></p>
	<h6>&nbsp&nbsp&nbsp<i><%= item.days_left %> days left to be completed!</i></h6>
<% end %>